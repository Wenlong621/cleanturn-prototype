# CleanTurn 清洁排程与房务回报系统 — 产品需求文档（PRD）

| 项 | 内容 |
|---|---|
| 版本 | v1.0（草案） |
| 日期 | 2026-07-26 |
| 状态 | 待业主确认（开放问题见第 10 节） |
| 来源 | 原始需求书 +《清潔人員app Dev》12 条业主答复整理 |
| 产品代号 | CleanTurn（暂定名） |

> 隐私说明：本文档及原型中的房源、人员、门锁密码、价格均为虚构示例。真实门锁密码、Wi-Fi 密码、房源地址等敏感信息不得写入公开文档。

---

## 1. 背景与问题

业主在温哥华运营约 20+ 套短租房源，现有工具链：

- **Hostfully**（PMS）：管理订单与渠道（Airbnb / Booking 等）。
- **Turno**：清洁任务管理（主/次清洁人员、自动指派、每单价格）。
- **智能门锁**：按订单自动生成/销毁门锁密码，有门锁日志。
- **电话 + WeChat**：清洁异常沟通。

### 现状痛点

1. Turno 的管理端 dashboard 信息组织差，管理员无法快速掌握当日全局（原话："目前的 dashboard 很废物"）。
2. 缺少**任务事件流水账**：损坏、维修、补货、额外清洁等事件无法以"叙述 + 照片 + 费用"结构化记录（业主调研后认为市面软件均无此功能，为本项目最重要的差异化点）。
3. 照片管理不符合需求：无法按类型区分保留期限，无法打包下载留证。
4. 异常沟通依赖 WeChat / 电话，记录散落、聊天体积膨胀，希望彻底摆脱。

### 项目定位

**自建一套替代 Turno 的内部清洁排程系统**，与 Hostfully 深度集成，复刻 Turno 的核心指派模型（主/次清洁人员 + 自动归属 + 每单价格），并补齐差异化能力（事件流水账、分级照片保留、可用的管理 dashboard、站内通知）。

---

## 2. 目标与非目标

### 目标

1. 订单 → 清洁任务全自动生成与联动更新，取代人工逐笔通知。
2. 清洁人员只看到被授权房源的任务（数据库层强制隔离）。
3. 手机优先的互动式月历 + 任务详情 + 状态回报 + 照片上传。
4. 任务事件流水账：叙述 + 照片 + 费用，可追溯、可导出。
5. 照片按 tag 分级保留并自动删除；异常照片可打包下载。
6. 管理员 dashboard：当日全局、待处理队列、异常、同步健康一屏掌握。
7. 全流程操作留痕（审计日志），异常沟通不再依赖 WeChat。

### 非目标（明确不做）

- 中期清洁 / 定期保养排程（业主 Turno 中该功能为关闭状态，确认不需要）。
- 多人协作同一任务的自动分账（以事件流水账替代）。
- 面向房客的任何界面。
- 渠道管理 / 订单创建（保留在 Hostfully）。
- 清洁工资自动支付（价格仅记录，结算报表为 P2）。

---

## 3. 用户角色与权限矩阵

| 能力 | 超级管理员 | 营运管理员 | 清洁人员 |
|---|---|---|---|
| PMS 连线配置（API Key / Webhook） | ✅ | ❌ | ❌ |
| 房源建立 / 编辑 / 停用 | ✅ | ✅ | ❌ |
| 房源进入信息（门锁/Wi-Fi/停车）编辑 | ✅ | ✅ | 只读 |
| 账号建立与角色分配 | ✅ | ❌ | ❌ |
| 主/次清洁人员与价格设置 | ✅ | ✅ | ❌ |
| 查看全部任务 / 调整指派 | ✅ | ✅ | ❌ |
| 查看/处理异常与事件流水账 | ✅ | ✅ | 本人任务可写 |
| 查看照片 / 打包下载 / 延长保留 | ✅ | ✅ | 本人任务可传可看 |
| 任务状态操作（接受/拒绝/开始/完成/异常） | 代操作 | 代操作 | ✅（仅本人） |
| 房源日志（换锁等观察记录） | ✅ | ✅ | 可新增，不可删改 |
| 审计日志查看 | ✅ | ✅ | ❌ |

- 每位清洁人员可被授权 1 个或多个房源；授权与撤销**即时生效**。
- 任何权限、价格、进入信息变更写入审计日志（操作者、时间、变更前后值）。
- 房客信息展示：任务详情向清洁人员显示房客姓名与人数（业主确认需要，用于同日入住客人提前放行李的场景）；不显示联系方式。

---

## 4. 核心用户旅程

### J1 清洁人员的一天
6:50 打开 App → 今日视图显示 2 个任务（1 个当日退房+入住的紧急任务置顶）→ 点开任务：11:00 退房 / 16:00 新客入住、4 位房客、门锁密码只读展示 → 11:05 到场点「开始清洁」→ 发现洗手液耗尽，添加补货事件（描述+照片）→ 清洁完成上传 3 张完成照 → 按大号「完成清洁」按钮 → 二次确认 → 完成。

### J2 拒单自动转派
新任务生成 → 推送给主要清洁人员 → 其点「拒绝」→ 系统即时转派次要清洁人员并通知管理员 → 次要人员接受 → 正常执行。若次要人员也拒绝或超时未响应 → 进入管理员待处理队列（见 OQ-1）。

### J3 异常处理闭环
清洁中发现马桶堵塞 → 清洁人员将任务标记「异常」，填写描述 + 上传异常照片（tag=异常，保留 ≥7 天）+ 记录疏通费用 → 管理员 dashboard 异常队列即时出现 → 管理员跟进处理 → 打包下载照片留证 → 标记异常已处理关闭。全程无 WeChat。

---

## 5. 功能需求（FR）

优先级：P0 = MVP 必须；P1 = 第二里程碑；P2 = 后续。

### FR-1 Hostfully 同步 [P0]

| # | 需求 | 说明 |
|---|---|---|
| 1.1 | Webhook 接入 | 接收 Hostfully webhook（HTTPS、返回 200、JSON/form-encoded），验证来源。需在 dev.hostfully.com 核对订单取消/修改事件覆盖（OQ-3） |
| 1.2 | 定时兜底同步 | 每日 3 次全量对账：21:00 / 06:00 / 10:00（America/Vancouver），业主指定节奏；替代原稿"每 5–15 分钟轮询" |
| 1.3 | 手动同步 | 管理端一键触发即时同步 |
| 1.4 | 幂等 upsert | 以 Hostfully UID 为外键，同步不产生重复订单/任务 |
| 1.5 | 字段拉取 | 房源 UID/名称、订单 UID、入住/退房日期时间、人数、房客姓名、订单状态（confirmed/cancelled/modified…） |
| 1.6 | 取消联动 | 订单取消 → 对应任务自动标记 Cancelled 并通知已指派人员 |
| 1.7 | 改期联动 | 订单日期变更 → 任务日期同步变更（**包括已接受/已开始的任务**），并向清洁人员与管理员推送变更提醒（原日期 → 新日期） |
| 1.8 | 同步健康 | dashboard 展示最近 webhook 事件、各兜底批次结果、失败告警 |

### FR-2 房源管理 [P0]

- 房源字段：编号/别名、区域简称、卧室/床/卫数量、时区、币种（CAD）、标准退房时间（默认 11:00）、标准入住时间（默认 16:00）、状态（启用/停用）。
- **进入信息**（管理员写 / 清洁人员只读）：门锁密码、车库密码、储物间密码、Wi-Fi 名称与密码、停车说明、进入说明。界面默认遮罩，点击显示。
- **每次清洁价格**：按房源 × 清洁人员设置（如 CA$300/次），仅记录用于对账，结算报表 P2。
- **房源日志**：时间线记录。系统自动条目（价格调整、人员变更、进入信息修改）+ 手动条目（管理员与清洁人员均可新增，如"7/21 换锁"），手动条目不可删改（可追加更正）。

### FR-3 指派模型 [P0]

- 每房源两个人员栏位：**主要清洁人员** + **次要清洁人员**（可空）；同一人可服务多个房源。
- **自动归属开关**（按房源）：开 = 新任务自动指派给主要清洁人员；关 = 新任务进入管理员待指派队列，逐单手动指派。
- **接受/拒绝流**：被指派人须「接受」任务；「拒绝」→ 自动转派次要人员 + 通知管理员；次要拒绝或无次要 → 管理员待处理队列。
- 超时未响应视同拒绝：建议默认 4 小时，back-to-back 任务 1 小时（OQ-1，待业主确认）。
- 权限模型：清洁人员可见范围 = 被授权房源集合；任务操作权 = 当前被指派人。授权即时生效，变更记审计。

### FR-4 任务生成与状态机 [P0]

- 每笔 confirmed 订单的**退房日**自动生成 1 笔 checkout 清洁任务（入住前检查等其他类型暂不生成）。
- **逾期判定**：任务截止 = 该房源下一笔订单入住时间（标准 16:00）；无下一笔订单时当日不判逾期，仅当日提醒。
- **back-to-back 标记**：退房日 = 下一订单入住日时任务标记「当日入住」，列表置顶 + 醒目提示。
- 任务状态机：

```
Offered(待接受) ──接受──▶ Pending(未开始) ──开始──▶ InProgress(清洁中) ──完成──▶ Completed(已完成)
     │                                                    │
     │拒绝/超时                                            ├──回报异常──▶ IssueReported(异常) ──管理员关闭──▶ Completed/Closed
     ▼                                                    │
 转派次要 ─再拒绝▶ 管理员待处理                             │
                                              订单取消（任意状态）──▶ Cancelled(已取消)
```

- 「完成清洁」为**大号按钮 + 二次确认弹窗**，防误触（业主明确要求）。
- 异常状态必须附文字说明，可附照片与费用（走事件流水账）。
- 所有状态变更记录操作者与时间戳。

### FR-5 清洁人员端（手机优先）[P0]

- **月历首页**：月视图，每日显示任务数徽标；点日期展开当日任务列表；支持"今日"快捷视图。
- **任务卡片**：房源别名、区域、退房时间 → 下次入住时间、房客人数、状态、back-to-back 徽标。
- **任务详情**：上述字段 + 房客姓名、进入信息（只读、默认遮罩）、备注、照片区、事件流水账、状态操作按钮。
- **变更提醒**：被改期/取消的任务在列表与详情展示变更横幅（原日期 → 新日期），并推送通知。
- 界面要求：字体清晰、按钮大、加载快（首屏 < 2s）、中文界面。

### FR-6 事件流水账 [P0]（差异化核心）

- 任务详情内可追加多条事件，每条包含：**类型**（损坏 / 维修 / 补货 / 额外清洁 / 遗留物 / 其他）+ **文字叙述** + **照片（多张，tag=异常）** + **费用（CAD，可为 0）** + 记录人 + 时间戳。
- 事件不可删除，可追加更正说明。
- 管理端可按房源/时间/类型筛选浏览，并导出 CSV（P1）。
- 用途：替代多人分账（临时协助以事件+费用记录）、维修留证、月度对账。

### FR-7 照片管理 [P0；转存 P2]

| # | 需求 | 说明 |
|---|---|---|
| 7.1 | 上传与绑定 | 照片绑定任务 ID（事件照片同时绑定事件 ID），记录上传人与时间戳 |
| 7.2 | tag 分级 | `完成照`（日常清洁完成）/ `异常照`（损坏、淹水、遗留物等） |
| 7.3 | 保留策略 | 完成照 **3 天**后自动永久删除；异常照 **≥7 天**，管理员可按件延长（默认延至异常关闭 + 7 天） |
| 7.4 | 彻底删除 | 到期由系统物理删除文件（含缩略图与 CDN 缓存失效），前端同步移除；删除后任何端不可再读取。签名 URL 短时效仅用于访问控制，不作为删除机制 |
| 7.5 | 打包下载 | 管理员可按任务/事件一键打包下载（zip） |
| 7.6 | 云盘转存 [P2] | 到期前转存 Google Drive / Dropbox（OAuth）；不接入国内网盘（业主明确排除） |

### FR-8 通知 [P1，触发矩阵 P0 预留]

| 事件 | 清洁人员 | 管理员 |
|---|---|---|
| 新任务待接受 | ✅ | — |
| 拒单转派 / 进入待处理 | 次要人员 ✅ | ✅ |
| 当日任务晨间提醒 | ✅ | 当日总览 |
| 订单改期/取消 | ✅（受影响人） | ✅ |
| 逾期风险（截止前 2h 未完成） | ✅ | ✅ |
| 异常上报 | — | ✅ |

- 渠道（OQ-2）：建议 PWA Web Push 为主 + Email 兜底；**明确不使用 WeChat**（业主要求摆脱）。SMS 备选（温哥华本地号码，成本低、必达性高）。
- MVP 阶段以站内通知中心 + dashboard 待处理队列先行。

### FR-9 管理员 Dashboard [P0]

- **KPI 磁贴**：今日任务、清洁中、待接受、异常待处理、逾期风险。
- **今日排程表**：房源、清洁人员、退房 → 下次入住窗口、back-to-back 徽标、状态。
- **待处理队列**：拒单转派、超时未响应、待手动指派。
- **异常动态**：最新异常事件（含照片缩略、费用），一键进入处理。
- **同步健康卡**：最近 webhook、兜底批次结果、手动同步按钮。
- 次级页面：任务总表（筛选/搜索）、房源管理、人员与授权、照片库、审计日志。

### FR-10 审计与安全 [P0]

- 审计事件：登录、权限/授权变更、价格变更、进入信息变更、任务状态变更、照片上传/删除/下载、同步批次。
- 认证：邮箱 + 密码 / Magic Link；会话过期策略；全站 HTTPS。
- 数据隔离：数据库行级安全（RLS）强制清洁人员仅能读取被授权房源数据（不依赖前端过滤）。
- 房客个人信息最小化：仅姓名 + 人数，不同步联系方式。

---

## 6. 非功能需求

| 类别 | 要求 |
|---|---|
| 设备 | 手机优先（清洁人员 100% 手机操作）；管理端适配桌面 |
| 性能 | 月历与任务列表首屏 < 2s；图片懒加载、压缩上传（长边 ≤ 2000px） |
| 可用性 | 大按钮、高对比度、中文界面；关键操作二次确认 |
| 时区 | 全系统 America/Vancouver；日期存储 UTC + 时区渲染 |
| 留痕 | 所有写操作带操作者与时间戳 |
| 合规 | 照片删除后不可恢复、不可访问；敏感字段（门锁密码）加密存储 |
| 弱网 | 照片上传失败自动重试 + 草稿暂存（OQ-5，MVP 做重试即可） |

---

## 7. 数据模型（概要）

```
users            (id, role[super_admin|ops_admin|cleaner], name, email, status)
properties       (id, hostfully_uid, alias, area, beds…, checkout_time=11:00,
                  checkin_time=16:00, status)
property_access  (property_id, cleaner_id, slot[primary|backup],
                  price_per_clean, auto_assign, granted_by, granted_at)   ← 授权+价格
property_secrets (property_id, door_code, garage_code, wifi_ssid, wifi_pwd,
                  parking_note, entry_note)  ← 加密存储，cleaner 只读
property_logs    (id, property_id, type[system|manual], body, author_id, created_at)
bookings         (id, hostfully_uid, property_id, guest_name, guests_count,
                  checkin_at, checkout_at, status, raw_payload, synced_at)
tasks            (id, booking_id, property_id, due_at, assignee_id,
                  assign_state[offered|accepted|declined|escalated],
                  status[offered|pending|in_progress|completed|issue|cancelled],
                  is_back_to_back, note, timestamps…)
task_events      (id, task_id, type[damage|repair|restock|extra_clean|lost_found|other],
                  body, cost_cad, author_id, created_at)   ← 事件流水账
photos           (id, task_id, event_id?, tag[routine|issue], storage_key,
                  uploaded_by, uploaded_at, expires_at, deleted_at)
notifications    (id, user_id, kind, payload, read_at, created_at)
audit_logs       (id, actor_id, action, entity, before, after, created_at)
sync_runs        (id, kind[webhook|cron|manual], status, changes_count, ran_at, detail)
```

- 照片删除：每日任务扫 `expires_at < now()` → 删除 Storage 对象 + 置 `deleted_at`，前端只渲染未删除记录。
- RLS 核心策略：`cleaner` 角色对 tasks/photos/bookings 的读取条件 = `property_id ∈ (select property_id from property_access where cleaner_id = auth.uid())`。

---

## 8. 技术架构（建议）

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | Next.js（PWA）+ FullCalendar 或自绘月历 | 手机优先；月历成熟；后续可加 Web Push |
| 后端/数据库 | Supabase（Postgres + RLS + Auth + Storage + Edge Functions + pg_cron） | RLS 天然实现房源级数据隔离；全家桶覆盖本项目全部需求；20+ 房源规模绰绰有余 |
| Hostfully 集成 | Edge Function 接 webhook（校验签名）+ pg_cron 定时对账（21:00/06:00/10:00）+ 手动触发 | webhook 为主、定时兜底，双保险 |
| 照片 | Supabase Storage（或 S3）+ 上传压缩 + 每日删除任务 | 生命周期由 DB `expires_at` 驱动，避免依赖存储层规则的粒度限制 |
| 部署 | Vercel + Supabase 云 | 零运维 |

预估规模：20+ 房源、2–5 名清洁人员、每月数百任务 —— 单区域单实例即可，无扩展性风险。

---

## 9. 里程碑

| 里程碑 | 范围 | 预估 |
|---|---|---|
| **M1 · MVP** | FR-1（webhook+兜底+手动）、FR-2（房源+进入信息+价格）、FR-3、FR-4、FR-5、FR-6、FR-7（tag+保留+删除+打包下载）、FR-9、FR-10；站内通知中心 | 3–4 周 |
| **M2** | FR-8 推送（Web Push + Email）、事件导出 CSV、审计 UI 完整化、房源日志完善、上传重试增强 | +2 周 |
| **M3** | FR-7.6 云盘转存、清洁费用月度结算报表、繁体界面切换 | 按需 |

---

## 10. 开放问题（需业主拍板）

| # | 问题 | 建议默认值 |
|---|---|---|
| OQ-1 | 接受任务的超时时长；次要人员也拒绝后的兜底流程 | 普通 4h / back-to-back 1h 未响应视同拒绝；兜底进管理员待处理并推送 |
| OQ-2 | 清洁人员通知渠道 | PWA Web Push 为主 + Email 兜底；SMS 备选 |
| OQ-3 | Hostfully webhook 是否覆盖订单修改/取消事件（需在 dev.hostfully.com 验证） | 若不覆盖，兜底同步频率提高为每 2h 一次 |
| OQ-4 | 云盘转存（Google Drive/Dropbox）优先级 | 放 M3；M1 以打包下载满足留证 |
| OQ-5 | 弱网离线要求深度 | M1 仅做上传失败重试；离线队列视实际反馈再排 |

---

## 11. 验收标准（MVP 关键场景）

1. Hostfully 新订单产生后 ≤ 兜底周期内，系统出现对应任务；webhook 正常时 ≤ 1 分钟。
2. 订单取消/改期后，任务状态/日期联动更新，且受影响清洁人员收到站内变更提醒（含原值 → 新值）。
3. 清洁人员 A 无法通过任何界面或 API 读取未授权房源的任务、照片、订单数据（RLS 层验证）。
4. 拒单后任务在 10 秒内转派次要人员，管理员待处理队列同步出现记录。
5. back-to-back 任务在清洁端与管理端均有明确"当日入住"标识并置顶。
6. 完成照在第 4 天 0 点前被物理删除且前端不可见；异常照第 7 天仍可访问、可打包下载。
7. 「完成清洁」必须经过二次确认；任务状态每次变更在审计日志可查（操作者 + 时间）。
8. 事件流水账可记录"叙述 + 2 张照片 + 费用"，管理端可按房源筛选查看。
9. 全部清洁端页面在 375px 宽手机上无横向滚动、可单手操作。
10. 权限撤销后清洁人员刷新即不可见对应房源任务（≤ 1 分钟生效）。
